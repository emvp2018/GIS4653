#Title: module
#Authors: Emily Wong, Jeff Clinard, Colton Snelling
#Description: This contains the functions to be called by the main module.

import arcpy
import os
from arcpy import env
env.workspace =

def year_select(): #Ask user to pick an election year - 2012 or 2016
    print 'Which election year would you like to use?: 2012 or 2016'
    x = raw_input('Provide Selection: ')
    if x == '2012':
        return x
    elif x == '2016':
        return x
    else:
        print 'Invalid Input'

def dissolve_tracts():
  arcpy.Dissolve_management("tracts.shp", "C:/Data/counties_dissolved.shp", countyID, 
  [["POP", SUM], ["AGE", MEAN"], ["SEX", SUM], ["MEDIAN_INCOME", MEAN]])
  
def join
census_join = AddJoin_management (tl_2010_06_tract10, GEOID, DEC_10_DP_DPDP1_with_ann, ID2,)

defcounty join
county
